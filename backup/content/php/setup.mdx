---
id: "44b01fdf-b0e8-4c18-b5bf-da5b117874ab"
title: "Setting Up PHP"
path: "php/setup"
status: "published"
meta_title: "Setting Up PHP | Learn Computer Academy"
meta_description: "How to get PHP running on your own computer using its built-in development server, and write your first Hello, World script."
sort_order: 2
updated_at: "2026-07-30T09:08:34.383221+00:00"
---

```json
[
  {
    "id": "oXhelJO2nUuk",
    "html": "<p>Because PHP runs on a server, you need something acting as a server to try it out — but that doesn't mean installing complicated software. PHP ships with a small <b>built-in development server</b> that's more than enough for learning and local testing.</p>",
    "type": "richtext"
  },
  {
    "id": "bIc6FIJwLqQ2",
    "text": "Installing PHP",
    "type": "heading",
    "level": 2,
    "anchor": "installing-php"
  },
  {
    "id": "UvyTbU7oRzXg",
    "html": "<p>Download PHP from <a href=\"https://www.php.net/downloads\" target=\"_blank\" rel=\"noopener noreferrer\">php.net/downloads</a> (Windows) or install it through your system's package manager (<code>brew install php</code> on macOS, <code>apt install php</code> on most Linux distributions). Once it's installed, confirm it worked by checking the version from a terminal:</p>",
    "type": "richtext"
  },
  {
    "id": "7yBDjL3SAXF1",
    "code": "php --version",
    "type": "code",
    "language": "bash",
    "runnable": false
  },
  {
    "id": "bSO_6b4V__dX",
    "html": "<p>If that prints a version number, PHP is ready to use.</p>",
    "type": "richtext"
  },
  {
    "id": "q2bw3MiD93k0",
    "text": "Your First Script",
    "type": "heading",
    "level": 2,
    "anchor": "your-first-script"
  },
  {
    "id": "D-gcOAzGhvjE",
    "html": "<p>Create a file named <code>index.php</code> with the following content:</p>",
    "type": "richtext"
  },
  {
    "id": "4Z9MCNGo9XOH",
    "code": "<?php\n  echo \"Hello, world!\";\n?>",
    "type": "code",
    "language": "php",
    "runnable": false
  },
  {
    "id": "pLObcLNJjnz_",
    "html": "<p>Every PHP script needs to be inside <code>&lt;?php ... ?&gt;</code> tags — that's how the server knows where PHP code starts and stops (you'll look at this properly in the next lesson). <code>echo</code> is the command that outputs text.</p>",
    "type": "richtext"
  },
  {
    "id": "jFTGfV2v6eUn",
    "text": "Running It with the Built-In Server",
    "type": "heading",
    "level": 2,
    "anchor": "running-it-with-the-built-in-server"
  },
  {
    "id": "Oeh9JsG8iHW-",
    "html": "<p>From the same folder as <code>index.php</code>, run:</p>",
    "type": "richtext"
  },
  {
    "id": "xenW6dHzpHpE",
    "code": "php -S localhost:8000",
    "type": "code",
    "language": "bash",
    "runnable": false
  },
  {
    "id": "3hvYJ70N-Bu3",
    "html": "<p>Then open <code>http://localhost:8000</code> in a browser. You should see <b>Hello, world!</b> — that text was generated by PHP running on your machine, not typed directly into an HTML file.</p>",
    "type": "richtext"
  },
  {
    "id": "410h3LJ2R7zY",
    "html": "<p>The built-in server (<code>php -S</code>) is perfect for learning and local development, but it's not meant for a real, public website — a live site needs a proper web server like Apache or Nginx alongside PHP. That's a deployment topic, not something you need to worry about while learning the language itself.</p>",
    "type": "callout",
    "title": "Built-in server = local only",
    "variant": "tip"
  },
  {
    "id": "tohZ-A_HlvEu",
    "html": "<p>With PHP actually running, the next lesson looks at the syntax rules you just used — the <code>&lt;?php ?&gt;</code> tags, statements, and comments — in proper detail.</p>",
    "type": "richtext"
  }
]
```
