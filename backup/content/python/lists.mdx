---
id: "cf79b55a-952b-4191-b888-2c91ebad8080"
title: "Lists"
path: "python/lists"
status: "published"
meta_title: "Python Lists | Learn Computer Academy"
meta_description: "Python's list type — an ordered, changeable collection — and the methods you'll use to work with one."
sort_order: 10
updated_at: "2026-08-04T11:39:29.450721+00:00"
---

```json
[
  {
    "id": "lt5OsYHOT5F0",
    "html": "<p>A <b>list</b> is Python's ordered, changeable collection — by far the most commonly used collection type.</p>",
    "type": "richtext"
  },
  {
    "id": "xe8N2nZuc66T",
    "text": "Creating a List",
    "type": "heading",
    "level": 2,
    "anchor": "creating-a-list"
  },
  {
    "id": "pHgY8RhqOK8W",
    "code": "fruits = [\"apple\", \"banana\", \"mango\"]\nprint(fruits[0])  # \"apple\"\nprint(fruits[2])  # \"mango\"\nprint(fruits[-1]) # \"mango\" — negative indexes count from the end",
    "type": "code",
    "language": "python",
    "runnable": false
  },
  {
    "id": "eI-kPcJ5GWQn",
    "text": "Modifying a List",
    "type": "heading",
    "level": 2,
    "anchor": "modifying-a-list"
  },
  {
    "id": "i0au2je3Ukp7",
    "code": "fruits = [\"apple\", \"banana\"]\nfruits.append(\"mango\")      # adds to the end\nfruits[0] = \"green apple\"   # overwrites index 0\nfruits.remove(\"banana\")     # removes the first \"banana\" found\ndel fruits[0]                # removes by index instead",
    "type": "code",
    "language": "python",
    "runnable": false
  },
  {
    "id": "RY5HcFK3hdVH",
    "text": "Common List Methods",
    "type": "heading",
    "level": 2,
    "anchor": "common-list-methods"
  },
  {
    "id": "TLxNlb9K2tIv",
    "rows": [
      [
        "list.append(x)",
        "Adds x to the end"
      ],
      [
        "list.insert(i, x)",
        "Inserts x at a specific index"
      ],
      [
        "list.pop(i)",
        "Removes and returns the item at index i (last item if i is omitted)"
      ],
      [
        "list.extend(other)",
        "Appends every item from another list"
      ],
      [
        "list.index(x)",
        "Index of the first x found (raises an error if missing)"
      ],
      [
        "list.count(x)",
        "How many times x appears"
      ],
      [
        "list.sort()",
        "Sorts the list in place"
      ],
      [
        "list.reverse()",
        "Reverses the list in place"
      ],
      [
        "list.clear()",
        "Removes every item, leaving an empty list"
      ],
      [
        "len(list)",
        "Number of items (a function, like with strings)"
      ]
    ],
    "type": "table",
    "header": [
      "Method",
      "What it does"
    ]
  },
  {
    "id": "pQr-JAF7ufLu",
    "text": "Looping Over a List",
    "type": "heading",
    "level": 2,
    "anchor": "looping-over-a-list"
  },
  {
    "id": "rNpQxozMxQo5",
    "code": "fruits = [\"apple\", \"banana\", \"mango\"]\nfor fruit in fruits:\n    print(fruit)\n\n# with the index too:\nfor index, fruit in enumerate(fruits):\n    print(index, fruit)",
    "type": "code",
    "language": "python",
    "runnable": false
  },
  {
    "id": "025FYw4CQGSP",
    "html": "<p><code>for x in list</code> is by far the most common way to loop in Python — you'll see it used constantly from here on. The Loops lesson later in this section covers Python's loops in full.</p>",
    "type": "richtext"
  },
  {
    "id": "vBGPSFgX6Aa8",
    "text": "Slicing",
    "type": "heading",
    "level": 2,
    "anchor": "slicing"
  },
  {
    "id": "zObSBzEpPB6s",
    "html": "<p>The same <code>[start:end]</code> slicing from the Strings lesson works on lists too — this is a general Python pattern, not something specific to strings:</p>",
    "type": "richtext"
  },
  {
    "id": "HfJk2pcT1UN1",
    "code": "numbers = [10, 20, 30, 40, 50]\nprint(numbers[1:3])  # [20, 30]\nprint(numbers[:2])   # [10, 20]\nprint(numbers[-2:])  # [40, 50]",
    "type": "code",
    "language": "python",
    "runnable": false
  },
  {
    "id": "dt5lT58IZsnk",
    "text": "Copying a List Safely",
    "type": "heading",
    "level": 2,
    "anchor": "copying-a-list-safely"
  },
  {
    "id": "Twv1CCmNmok-",
    "html": "<p>Assigning a list to a new name doesn't copy it — both names point at the exact same list, so changing one changes the other:</p>",
    "type": "richtext"
  },
  {
    "id": "Eq8P8h0aIcyN",
    "code": "original = [1, 2, 3]\nalias = original\nalias.append(4)\nprint(original)  # [1, 2, 3, 4] — original changed too, since it's the same list",
    "type": "code",
    "language": "python",
    "runnable": false
  },
  {
    "id": "sEqiJLgkRzva",
    "html": "<p><code>.copy()</code>, or the equivalent <code>list(original)</code>, creates a genuinely separate list:</p>",
    "type": "richtext"
  },
  {
    "id": "b6FlcmVWb59u",
    "code": "original = [1, 2, 3]\ncopy = original.copy()\ncopy.append(4)\nprint(original)  # [1, 2, 3] — unaffected\nprint(copy)      # [1, 2, 3, 4]",
    "type": "code",
    "language": "python",
    "runnable": false
  }
]
```
