---
id: "aab0100d-7926-425b-8796-9085a42b6e91"
title: "Making HTTP Requests from Node.js"
path: "nodejs/making-http-requests-with-fetch"
status: "published"
meta_title: "Making HTTP Requests from Node.js | Learn Computer Academy"
meta_description: "Calling another API from a Node script or server using the built-in global fetch — no dependency required, GET and POST requests, and error handling."
sort_order: 30
updated_at: "2026-09-03T04:41:42.847105+00:00"
---

```json
[
  {
    "id": "b3PEOtLuVjf1",
    "html": "<p>Every earlier lesson in this course built a server that <i>receives</i> requests. This lesson is the other direction — a Node script or server calling <i>another</i> API, using the same <code>fetch</code> function already familiar from browser JavaScript.</p>",
    "type": "richtext"
  },
  {
    "id": "8qFULGhXaFmw",
    "text": "fetch Is Built In",
    "type": "heading",
    "level": 2,
    "anchor": "fetch-is-built-in"
  },
  {
    "id": "_KigkVtcxJiO",
    "html": "<p>Node 18 and later ships a global <code>fetch</code> — no package to install, no <code>import</code> needed.</p>",
    "type": "richtext"
  },
  {
    "id": "S7OXoWoL360f",
    "code": "const response = await fetch('https://api.example.com/users')\nconst users = await response.json()\nconsole.log(users)",
    "type": "code",
    "language": "js",
    "runnable": false
  },
  {
    "id": "VFBOnXP5BMzK",
    "text": "Checking the Response Status",
    "type": "heading",
    "level": 2,
    "anchor": "checking-the-response-status"
  },
  {
    "id": "dJMuMAS9_HPk",
    "html": "<p>Unlike most Node error handling, <code>fetch</code> does <i>not</i> throw for a 404 or 500 response — only for a genuine network failure. The status needs an explicit check.</p>",
    "type": "richtext"
  },
  {
    "id": "VvRq71rPlHvV",
    "code": "const response = await fetch('https://api.example.com/users/999')\n\nif (!response.ok) {\n  throw new Error(`Request failed: ${response.status}`)\n}\n\nconst user = await response.json()",
    "type": "code",
    "language": "js",
    "runnable": false
  },
  {
    "id": "gM0-VtPgN2lQ",
    "text": "A POST Request, Sending JSON",
    "type": "heading",
    "level": 2,
    "anchor": "a-post-request-sending-json"
  },
  {
    "id": "yZ6GORE8LiL3",
    "code": "const response = await fetch('https://api.example.com/users', {\n  method: 'POST',\n  headers: { 'Content-Type': 'application/json' },\n  body: JSON.stringify({ name: 'Sam', email: 'sam@example.com' }),\n})\n\nconst created = await response.json()",
    "type": "code",
    "language": "js",
    "runnable": false
  },
  {
    "id": "xa5cM9Liw_oh",
    "text": "Adding Authentication",
    "type": "heading",
    "level": 2,
    "anchor": "adding-authentication"
  },
  {
    "id": "cZmeogJCVz_W",
    "code": "const response = await fetch('https://api.example.com/orders', {\n  headers: { Authorization: `Bearer ${apiToken}` },\n})",
    "type": "code",
    "language": "js",
    "runnable": false
  },
  {
    "id": "7jcVMZfKkxCi",
    "text": "A Realistic Wrapper Function",
    "type": "heading",
    "level": 2,
    "anchor": "a-realistic-wrapper-function"
  },
  {
    "id": "Xpi9ywMgmgcZ",
    "code": "async function apiGet(path) {\n  const response = await fetch(`https://api.example.com${path}`, {\n    headers: { Authorization: `Bearer ${process.env.API_TOKEN}` },\n  })\n  if (!response.ok) {\n    throw new Error(`API error ${response.status}: ${await response.text()}`)\n  }\n  return response.json()\n}\n\nconst orders = await apiGet('/orders')",
    "type": "code",
    "language": "js",
    "runnable": false
  },
  {
    "id": "1wfutzL9EJb1",
    "html": "<p>Third-party libraries like axios still add real conveniences (automatic JSON parsing, request/response interceptors, built-in timeout handling) — worth reaching for on a larger project. Built-in fetch is enough for most everyday cases without adding a dependency.</p>",
    "type": "callout",
    "title": "When a library still helps",
    "variant": "tip"
  }
]
```
