import type { Locale } from './types'

// EN + BN strings for the Recursion Visualizer (docs/TOOLS.md house rule 1)
// — JS keyword/function names stay English inside Bengali sentences, same
// convention as every other tool's i18n file.
export const RECURSION_STRINGS = {
  en: {
    title: 'Recursion Visualizer',
    subtitle: 'Watch a recursive function’s call stack really grow and shrink, with the real arguments and real return value shown on every frame — and the exact moment it unwinds back into an answer.',
    lessonCta: 'Browse Programming lessons',
    realNote: 'This runs your real code in a sandboxed iframe — the stack you see is genuinely how deep the calls went, not a hand-drawn animation. Presets are pre-wrapped with a small trace() helper so arguments and return values can be captured; switch to "Edit code" to try your own, following the same wrapping shown there.',
    preset: 'Example',
    presetSum: 'Sum 1..n',
    presetFactorial: 'Factorial',
    presetFibonacci: 'Fibonacci',
    input: 'n',
    run: 'Run',
    running: 'Running…',
    editCode: 'Edit code',
    code: 'Code',
    visualization: 'Visualization',
    callStack: 'Call Stack',
    callStackEmpty: 'empty',
    callLog: 'Call Log',
    callLogEmpty: 'nothing returned yet',
    stats: 'Stats',
    totalCalls: 'Total calls',
    maxDepth: 'Max depth reached',
    step: 'Step',
    of: 'of',
    prev: 'Previous',
    next: 'Next',
    play: 'Play',
    pause: 'Pause',
    reset: 'Restart',
    speed: 'Speed',
    narration: 'What’s happening',
    narrateCall: '{name}({args}) is called — pushed onto the stack.',
    narrateReturn: '{name}({args}) returns {result} — popped off the stack.',
    narrateError: 'Stopped: {text}',
    errors: 'Error',
    howItWorks: 'How this works',
    howItWorksBody: 'Getting a function’s real arguments and return value — not just its name — needs the traced function wrapped in a small trace() helper, which the three presets already do (a let x; x = trace("x", function (n) {...}) pattern, so the function’s own recursive calls go through the traced version, not an untraced inner copy of itself). That’s why this tool ships as presets plus an editable-code escape hatch, rather than trying to auto-trace arbitrary free-form code the way the Event Loop Visualizer does — a full JS parser would be needed to rewrite arbitrary call sites safely, and this tool doesn’t have one. The call stack panel, ordering and every argument/return value shown are real, recorded from your code actually running — not invented for the demo.',
  },
  bn: {
    title: 'Recursion ভিজুয়ালাইজার',
    subtitle: 'একটা recursive function-এর call stack সত্যিই বাড়তে আর কমতে দেখুন, প্রতিটা frame-এ আসল argument আর আসল return value দেখানোসহ — আর ঠিক যে মুহূর্তে এটা unwind করে একটা উত্তরে পরিণত হয়।',
    lessonCta: 'Programming পাঠ দেখুন',
    realNote: 'এটা আপনার আসল কোড একটা sandboxed iframe-এ চালায় — আপনি যে stack দেখছেন সেটা সত্যিই call কতটা গভীরে গিয়েছিল তার হিসাব, হাতে আঁকা animation না। Preset-গুলো একটা ছোট trace() helper দিয়ে আগে থেকে wrap করা আছে যাতে argument আর return value ধরা যায়; নিজের কোড চেষ্টা করতে "Edit code"-এ যান, একই wrapping অনুসরণ করে।',
    preset: 'উদাহরণ',
    presetSum: 'Sum 1..n',
    presetFactorial: 'Factorial',
    presetFibonacci: 'Fibonacci',
    input: 'n',
    run: 'Run করুন',
    running: 'চলছে…',
    editCode: 'কোড এডিট করুন',
    code: 'কোড',
    visualization: 'ভিজুয়ালাইজেশন',
    callStack: 'Call Stack',
    callStackEmpty: 'খালি',
    callLog: 'Call Log',
    callLogEmpty: 'এখনো কিছু return হয়নি',
    stats: 'Stats',
    totalCalls: 'মোট call',
    maxDepth: 'সর্বোচ্চ depth',
    step: 'Step',
    of: '/',
    prev: 'আগের',
    next: 'পরের',
    play: 'Play',
    pause: 'Pause',
    reset: 'আবার শুরু',
    speed: 'গতি',
    narration: 'কী হচ্ছে',
    narrateCall: '{name}({args}) কল হয়েছে — stack-এ push হয়েছে।',
    narrateReturn: '{name}({args}) {result} return করে — stack থেকে pop হয়ে যায়।',
    narrateError: 'থেমে গেছে: {text}',
    errors: 'Error',
    howItWorks: 'এটা যেভাবে কাজ করে',
    howItWorksBody: 'একটা function-এর আসল argument আর return value পাওয়া — শুধু নাম না — traced function-কে একটা ছোট trace() helper-এ wrap করতে হয়, যা তিনটা preset-ই আগে থেকে করে (একটা let x; x = trace("x", function (n) {...}) pattern, যাতে function-এর নিজের recursive call traced version দিয়েই যায়, নিজের একটা untraced ভেতরের কপি দিয়ে না)। এই কারণে এই টুল preset আর একটা editable-code বিকল্প নিয়ে আসে, Event Loop ভিজুয়ালাইজারের মতো যেকোনো free-form কোড auto-trace করার চেষ্টা করার বদলে — যেকোনো call site নিরাপদে rewrite করতে একটা পূর্ণাঙ্গ JS parser দরকার হতো, আর এই টুলের সেটা নেই। Call stack প্যানেল, order আর দেখানো প্রতিটা argument/return value আসল, আপনার কোড সত্যিই চলার সময় রেকর্ড করা — demo-র জন্য বানানো না।',
  },
} as const

export function rvs(locale: Locale) {
  return RECURSION_STRINGS[locale]
}
